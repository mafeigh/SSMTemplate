-- oracle
create table APPLY
(
  ID        NUMBER(19),
  NAME      VARCHAR2(50),
  MONEY     NUMBER(19,6),
  BIRTHDATE DATE
);

--mysql
create table APPLY
(
  ID        INT,
  NAME      varchar(50),
  MONEY     DOUBLE,
  BIRTHDATE DATE
);
